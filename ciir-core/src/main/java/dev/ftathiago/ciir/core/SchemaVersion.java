package dev.ftathiago.ciir.core;

/** The CIIR contract version this generator produces. */
public final class SchemaVersion {

  /** Backward-compatible additions bump the minor version; breaking changes bump the major. */
  public static final String CURRENT = "1.0";

  private SchemaVersion() {}
}
